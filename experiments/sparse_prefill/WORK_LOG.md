# 实验过程日志

采用追加记录：每条包含现象、证据、决策、验证与边界。
错误尝试也保留；代码提交负责记录可运行的变更，日志解释为什么这样改。
以下记录始于 2026-09-13，性能实验尚未完成。

## E001：建立共享机器的操作边界

- 只在用户授权的新目录中创建文件、环境、模型和缓存。
- 初次 SSH 登录后建立权限收紧的独立目录，保存只读探测报告。
- GPU 是 RTX4090 D；CPU 32 逻辑线程；内存总计约 46 GiB，
  探测时可用约 24 GiB；工作区所在磁盘剩余约 183 GB。
- 此时 GPU 管理接口失败，不能从硬件型号推断 GPU 已经可用。

## E002：nvidia-smi 失败并不只是监控问题

- 现象：`Failed to initialize NVML: Driver/library version mismatch`。
- 内核模块版本为 `580.173.02`；系统 NVML 和 libcuda 为 `580.178.04`。
- 标准库 ctypes 调用 `cuInit(0)` 返回
  `804 CUDA_ERROR_COMPAT_NOT_SUPPORTED_ON_DEVICE`。
- `/proc/self/maps` 证明实际加载的是较新的系统 libcuda，
  `LD_LIBRARY_PATH` 和 `LD_PRELOAD` 均未设置。
- 判断：GPU 计算入口也失败，不能通过安装 PyTorch 来掩盖此问题。

## E003：目录内修复，不升级共享系统

- 系统里没有可直接选择的 `580.173.02` 用户态库。
- 从 NVIDIA 官方 Ubuntu 22.04 仓库下载同版本 compute/gpucomp/utils 包，
  按 HTTPS Packages.gz 的 SHA256 字段验证，使用 `dpkg-deb --extract`
  解到工作目录，而非安装软件包或运行维护脚本。
- 只给实验子进程设置匹配的 `LD_LIBRARY_PATH`。
  没有使用 sudo、重启、卸载内核模块、修改系统软链接或停止其他进程。
- 验证：NVML 恢复；显存总计 24564 MiB，观测到空闲 23782 MiB；
  CUDA 初始化返回成功，映射路径指向工作区内 libcuda。
- 再验证实际计算：分配 4096 字节、PTX JIT、启动 kernel、同步、拷回，
  1024 个整数结果全部吻合。清除继承的库路径后用启动脚本重测也通过。
- 限制：这只打通了 CUDA，不代表已经验证 PyTorch、vLLM 或模型性能。
  启动脚本检查内核版本，系统升级后应重新验证该覆盖。

## D001：选范围时区分功能价值与性能证据

- ZipServ 的公开集成不完整，小矩阵与频繁权重更新有风险；
  Seer/DAS 的新增收益还依赖本项目未观测的同题轨迹相关性。
- 本项目选择调用者已有的动作位置信息，直接删除不需要的 LM-head 行。
- 不做新的训练流程来制造优化需求，定位为轨迹打分能力扩展。
- 投影行可省约 97% 的长度推导不意味着端到端同等收益；
  4B 模型静态线性层 FLOPs 推导约省 7.5%，没有提前承诺提速数字。

## D002：首版保留原输出索引，控制跨模块工程量

- 原候选包含逐位置候选集合和紧凑新协议，工程涉及更多输出通路。
- 首版只增加 `prompt_logprob_positions`，输出保留完整 prompt 索引和空槽。
- 好处：现有调用端按原始位置取分数，EngineCore 无需新增位置协议，
  正常请求可保留原来的执行路径。
- 代价：最终输出仍包含 O(L) 空槽；必须测真实序列化字节，
  不能沿用之前紧凑协议的总字节缩减预测。
- 非连续位置的 UTF-8 解码应使用真实相邻输入 Token，
  不能把前一个被选择动作当作紧邻上下文。

## E004：冻结版本与环境准备

- 最新稳定版 API 查询仍为 v0.29.0，源代码 SHA 与此前 fork 一致。
- 工作区内安装 uv 0.12.13，创建托管 CPython 3.12.14 和独立 `.venv`。
- vLLM 0.29.0、PyTorch/CUDA wheel 与开发工具正在下载。
  先使用预编译扩展，避免在共享机器上全量编译 CUDA。
- 下载完成前不把环境标记为可运行，也不写入模型性能成绩。

## E005：远程直连 GitHub 超时

- 远程浅克隆 vLLM 在约 136 秒后连接 github.com:443 超时。
- 官方 NVIDIA 和 Python 依赖下载路径此前可达，不能将一次超时
  直接判断为全部网络不可用，也不能靠无休止重试浪费时间。
- 决定从本地已固定版本的仓库生成源码归档和 Git 元数据，通过 SSH 传输。
  传输后核验 SHA 与工作树；保持真实 Git 历史，不把源码快照冒充新原创。
- 结果：SSH 传输完成，两个归档的 SHA256 在本地和远程一致；
  远程提交哈希与冻结基线一致。

## E006：macOS 归档元数据触发 Linux Git 报错

- `tar` 提示未知的 `LIBARCHIVE.xattr.com.apple.provenance` 扩展头；
  随后 Git 报 `._pack-....idx is too small`。
- 原因是复制 `.git` 时附带了 AppleDouble `._` 元数据，
  Linux Git 把其中的 `.idx` 后缀文件当成对象包索引。
- 仅删除这次归档生成的 `.git` 内 58 个 `._*` 文件及根部 `._.git`。
  清理后 `git status --short` 为空，提交 SHA 正确。
- 后续跨平台归档应关闭 macOS metadata/xattrs；
  不把这类归档问题误诊为需要重新创建 Git 历史。

## E007：依赖与模型下载路径的调整

- 远程 Hugging Face API 超时；一个模型镜像返回 HTTP 403，
  没有尝试绕过该端点的限制。
- 本地可以读取 Hugging Face 官方 metadata，获得 0.6B revision
  `c1899de289a04d12100db370d81485cdf75e47ca`。
- Qwen 在 ModelScope 的官方发布可达，`model.safetensors` 和
  `tokenizer.json` 的 SHA256 与 HF 官方 LFS metadata 一致。
- 使用 ModelScope 每个文件的 revision 和 SHA256 下载，
  不将更换下载路径混同于更换模型；下载脚本在重命名前验证内容。
- 原依赖源持续下载很慢；国内 PyPI 镜像索引约 0.61 秒响应。
  只中断本项目的 uv 安装进程，改为 4 路并行下载并复用已有缓存。
  所有包版本仍由同一个 vLLM/PyTorch 配置约束，完成后需保存实际版本清单。
- 状态：依赖与模型仍在下载，不能将“下载命令已启动”写成“环境安装成功”。

## D003：把测量开销与目标耗时分开

- 通过现有 `LLM.collective_rpc` 在 worker 内临时计数投影行并读取显存，
  用完还原原函数；正式计时不保留计数 hook。
- 主对照均启用已有 FlatLogprobs，避免只对低效的 Python 字典输出报喜。
- 保存合成轨迹、动作区间、每个动作分数、源文件哈希和逐轮时间。
- 完整请求耗时包括参数构造、原生 API 调用和必要尾部生成。
  JSON 字节只称为定义好的结果表示大小，不冒充实际 RPC 流量。
- 检测到其他 GPU 计算进程时不启动或中止正式计时，不停止别人任务。

## E008：首次静态检查反馈

- 新增代码首次 Ruff 检查发现导入顺序、超长行和可合并的嵌套条件。
- 格式工具修正布局，手动合并请求配置检查；再次 Ruff check/format 通过。
- 这只证明静态风格检查通过，不替代单元测试或真实 GPU 验证。
- 第一笔文档提交 `e82230e` 已通过仓库提交钩子并推送到个人 fork。

## D004：用固定 logits 隔离数值原因

- 在真实 GPU worker 测试中固定相同 logits，仅改变所选行。
  对照 FP32 全词表 log-softmax，覆盖目标分数、Top-2 和全词表输出。
- 该测试不让 GEMM 的批次形状变化混入位置映射或归一化检查。
  模型级误差另行记录，不事后放宽同一项测试来掩盖原因。
- 复用上游分块与强制抢占测试，增加动作位置参数；
  除了核对位置，还要求实际抢占计数大于零。
- 补充设计教学文档，写明因果偏移、packed batch 公式与验证理由。

## E009：完整提交检查发现测试类型问题

- Ruff、拼写、Markdown、源码类型检查和配置校验通过。
- mypy 发现抢占测试中可空 `positions` 直接参与 `in` 判断。
  在稀疏分支增加明确的不为空断言，局部 mypy 重跑通过。
- 增加非 CUDA 平台的对应测试跳过条件，避免测试与首版支持范围冲突。

## E010：大权重分段下载与版本一致性

- 远程读取 4B metadata 一次超时；改用本地可达的官方 API 读取，
  把小型 metadata 通过 SSH 传入工作目录。
- 4B 官方 HF revision 为 `1cfa9a7208912126459214e8b04321603b3df60c`，
  三个 safetensors 文件的大小与 SHA256 均与 Qwen 官方 ModelScope 发布一致。
- 一次有界的 1 MiB Range 请求返回 HTTP 206 和正确 Content-Range，
  证明可以用分段续传避免大文件连接失败后全部重下。
- 下载脚本限定最多 4 路，每段 32 MiB，检查响应范围和长度，
  完成后校验整个文件 SHA256，再替换最终文件；保留失败部分供续传。
- 该工作属于实验环境准备，不算 vLLM 功能或性能成果。

## E011：检查点提交与跨平台传输

- 实现、测试草稿和测量脚本提交为 `5f7b949`，全部仓库提交检查通过，
  已推送到个人 fork；这不表示运行验收完成。
- 远程通过 Git bundle 校验并取得相同提交，避免再次依赖远程 GitHub 连接。
- `COPYFILE_DISABLE=1` 阻止了 AppleDouble，但 tar 仍带有 provenance 扩展头，
  GNU tar 忽略这些头并告警。后续优先传 Git bundle；使用 tar 时还需禁用 xattrs。
- 同步保存实际驱动修复与 CUDA smoke 的源码、包校验清单和说明。

## D005：在观测结果前调整 4B 测量方式

- 此决策发生于模型实验开始前，没有看过任何正负性能结果。
- 4B 权重约 8 GB，当前网络会让下载占据很长时间。
  先终止本项目的 4B 下载进程，保留已校验长度的 768 MiB 完整分段以便续传，
  让依赖与 0.6B 正确性验收优先使用网络。
- 本地源码 `DummyModelLoader` 和 `initialize_dummy_weights` 已提供原生随机权重
  性能测量路径；使用固定的每参数随机种子和小范围随机浮点权重。
- 对本实验的稠密 Qwen3、固定输入长度、固定生成一步、关闭推测解码、
  `prompt_logprobs=0` 和禁用文本解码，算子形状与循环次数由结构决定。
  概率内核仍扫描完整词表，没有因分数值提前退出的路径。
- 因此可用真实 4B 结构与随机权重测执行成本，省去预训练权重传输。
  这不是 CPU 模拟器，仍执行真实 Transformer、LM head、概率内核和结果通路。
- 但不能据此保证运行时间与真实权重逐项相同。先对照 0.6B 的真实与随机权重，
  定量检查代理偏差；4B 结果明确标注随机权重，不作模型质量或真实权重成绩。
- 协议相应更新：真实权重结果以 0.6B 为准，4B 作为结构成本证据。
  两类结果分开报告，不选取更好的一组冒充同一个实验。

## E012：模型文件准备完成

- 0.6B 真实权重、配置与分词器全部通过 SHA256 校验。
  1.50 GB 权重的单连接下载约 2590 秒，保存了完整下载清单。
- 4B metadata 下载出现一次读超时，保留失败日志；
  将 0.6B 中与 4B 官方哈希一致的 tokenizer/vocab 文件在专用目录内复用。
  随后一次有界重试成功，metadata-only 清单已写入模型目录。
- 4B 配置确认：36 层，隐藏维度 2560，32 个 Q heads、8 个 KV heads，
  FFN 维度 9728，词表 151936；没有用小模型冒充 4B 结构。
- 依赖安装仍在下载最后的 vLLM/CUDA 包，没有将模型文件就绪当作运行成功。

## 开发中的未验收项

- 请求校验、GPU 行选择、跨块收集和 CPU 位置恢复已有工作区草稿，
  尚未通过单元测试或 GPU 集成验证。
- 待建立：原版模型基线、最小行为测试、误差报告、配对性能原始结果。
- 过程中使用 AI 辅助；最终简历描述必须限定到实际完成并验证的部分。
